import { createTheme } from "@mui/material";

export const darkTheme = createTheme({
  palette: {
    primary: {
      main: "#fff",
    },
    mainColor: {
      main: "#BB86FC",
    },
    mode: "dark",
  },
  typography: {
    fontFamily: "montserrat",
  },
});
